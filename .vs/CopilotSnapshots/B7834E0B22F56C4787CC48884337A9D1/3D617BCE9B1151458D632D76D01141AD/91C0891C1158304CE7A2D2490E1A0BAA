@using Application_Educative.Models
@model Etudiant

@{
    ViewData["Title"] = "Ajouter un Étudiant";
}
@model Etudiant

@{
    ViewData["Title"] = "Ajouter un Étudiant";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <h2>➕ Ajouter un Étudiant</h2>

            <form id="formEtudiant" enctype="multipart/form-data">
                <div class="form-group mb-3">
                    <label for="nom" class="form-label">Nom *</label>
                    <input type="text" class="form-control" id="nom" name="nom" placeholder="Ex: Dupont" required />
                    <small class="text-danger" id="nomError"></small>
                </div>

                <div class="form-group mb-3">
                    <label for="prenom" class="form-label">Prénom *</label>
                    <input type="text" class="form-control" id="prenom" name="prenom" placeholder="Ex: Jean" required />
                    <small class="text-danger" id="prenomError"></small>
                </div>

                <div class="form-group mb-3">
                    <label for="etat" class="form-label">État *</label>
                    <select class="form-control" id="etat" name="etat" required>
                        <option value="">-- Sélectionner --</option>
                        <option value="Admis">Admis</option>
                        <option value="Redoublant">Redoublant</option>
                    </select>
                </div>

                <div class="form-group mb-3">
                    <label for="tranche" class="form-label">Tranche *</label>
                    <select class="form-control" id="tranche" name="tranche" required>
                        <option value="">-- Sélectionner --</option>
                        <option value="1ère Tranche">1ère Tranche</option>
                        <option value="2ème Tranche">2ème Tranche</option>
                        <option value="3ème Tranche">3ème Tranche</option>
                        <option value="Totalité">Totalité Payée</option>
                    </select>
                </div>

                <div class="form-group mb-3">
                    <label for="section" class="form-label">Section *</label>
                    <select class="form-control" id="section" name="section" required>
                        <option value="">-- Sélectionner --</option>
                        <option value="1ère Année Cycle Ing">1ère Année Cycle Ing</option>
                    </select>
                </div>

                <div class="form-group mb-3">
                    <label for="specialite" class="form-label">Spécialité *</label>
                    <select class="form-control" id="specialite" name="specialite" required>
                        <option value="">-- Sélectionner --</option>
                        <option value="DevOps">DevOps</option>
                        <option value="GL">GL (Génie Logiciel)</option>
                        <option value="BI">BI (Business Intelligence)</option>
                    </select>
                </div>

                <div class="form-group mb-3">
                    <label for="motDePasse" class="form-label">Mot de Passe *</label>
                    <input type="password" class="form-control" id="motDePasse" name="motDePasse" placeholder="Min 6 caractères" required />
                    <small class="text-danger" id="mpError"></small>
                </div>

                <div class="form-group mb-3">
                    <label for="imageFile" class="form-label">Photo de l'Étudiant</label>
                    <input type="file" class="form-control" id="imageFile" name="imageFile" accept="image/*" />
                    <small class="text-muted">Formats acceptés: JPG, PNG, GIF (Max 5MB)</small>
                    <small class="text-danger" id="imageError"></small>
                </div>

                <div class="form-group">
                    <button type="submit" class="btn btn-success btn-lg">
                        <i class="fas fa-save"></i> Ajouter l'Étudiant
                    </button>
                    <a href="/GestionEtudiant/Index" class="btn btn-secondary btn-lg">
                        <i class="fas fa-times"></i> Annuler
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Modal Succès -->
<div class="modal fade" id="successModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-success text-white">
                <h5 class="modal-title">✅ Succès</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p id="successMessage"></p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-success" data-bs-dismiss="modal" onclick="location.href='/GestionEtudiant/Index'">
                    OK
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Modal Erreur -->
<div class="modal fade" id="errorModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title">❌ Erreur</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <ul id="errorList"></ul>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-bs-dismiss="modal">Fermer</button>
            </div>
        </div>
    </div>
</div>

<script>
    document.getElementById('formEtudiant').addEventListener('submit', async (e) => {
        e.preventDefault();

        // Réinitialiser les erreurs
        document.getElementById('nomError').textContent = '';
        document.getElementById('prenomError').textContent = '';
        document.getElementById('mpError').textContent = '';
        document.getElementById('imageError').textContent = '';

        const nom = document.getElementById('nom').value.trim();
        const prenom = document.getElementById('prenom').value.trim();
        const motDePasse = document.getElementById('motDePasse').value;
        const imageFile = document.getElementById('imageFile').files[0];

        let hasError = false;

        // Validation Nom (lettres uniquement)
        if (!/^[a-zA-ZÀ-ÿ\s'-]+$/.test(nom)) {
            document.getElementById('nomError').textContent = 'Le nom doit contenir uniquement des lettres';
            hasError = true;
        }

        // Validation Prénom (lettres uniquement)
        if (!/^[a-zA-ZÀ-ÿ\s'-]+$/.test(prenom)) {
            document.getElementById('prenomError').textContent = 'Le prénom doit contenir uniquement des lettres';
            hasError = true;
        }

        // Validation Mot de Passe (min 6 caractères)
        if (motDePasse.length < 6) {
            document.getElementById('mpError').textContent = 'Le mot de passe doit contenir au moins 6 caractères';
            hasError = true;
        }

        // Validation Image (optionnelle mais si fournie max 5MB)
        if (imageFile && imageFile.size > 5 * 1024 * 1024) {
            document.getElementById('imageError').textContent = 'L\'image dépasse 5MB';
            hasError = true;
        }

        if (hasError) {
            return;
        }

        // Envoi du formulaire
        const formData = new FormData(document.getElementById('formEtudiant'));

        try {
            const response = await fetch('/GestionEtudiant/Create', {
                method: 'POST',
                body: formData
            });

            const data = await response.json();

            if (data.success) {
                document.getElementById('successMessage').textContent = data.message;
                new bootstrap.Modal(document.getElementById('successModal')).show();
            } else {
                document.getElementById('errorList').innerHTML = data.errors
                    .map(err => `<li>${err}</li>`)
                    .join('');
                new bootstrap.Modal(document.getElementById('errorModal')).show();
            }
        } catch (error) {
            console.error('Erreur:', error);
            alert('Erreur lors de l\'envoi');
        }
    });
</script>